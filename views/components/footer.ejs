<style>
  :root {
    --footer-bg: #0d131f;
    --footer-surface: #151d2a;
    --footer-border: rgba(255, 255, 255, 0.08);
    --brand-primary: #f17649;
    --brand-hover: #ff8c42;
    --text-main: #f8fafc;
    --text-muted: #94a3b8;
  }

  /* Footer Base Canvas */
  footer {
    background-color: var(--footer-bg);
    color: var(--text-main);
    font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    border-top: 1px solid var(--footer-border);
    position: relative;
  }

  /* Top Accent Line */
  footer::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: linear-gradient(90deg, var(--brand-primary), #f06bad, transparent);
  }

  /* Main Section Grid */
  .footer-container {
    max-width: 1140px;
    margin: 0 auto;
    padding: 60px 24px 40px;
    display: grid;
    grid-template-columns: 2fr 1fr 1fr 1.2fr;
    gap: 3rem;
  }

  /* Column 1: Brand Info */
  .footer-brand {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
  }

  .footer-logo {
    max-height: 50px;
    width: 300px;
    object-fit: contain;
    margin-bottom: 1.2rem;
  }

  .footer-description {
    color: var(--text-muted);
    font-size: 0.925rem;
    line-height: 1.6;
    margin-bottom: 1.5rem;
  }

  /* Social Links Group */
  .social-group {
    display: flex;
    gap: 0.6rem;
  }

  .social-icon {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background-color: rgba(255, 255, 255, 0.04);
    border: 1px solid var(--footer-border);
    color: var(--text-muted) !important;
    display: flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
    font-size: 0.95rem;
    transition: all 0.2s ease;
  }

  .social-icon:hover {
    background-color: rgb(226, 77, 23);
    color: #ffffff !important;
    border-color: var(--brand-primary);
    transform: translateY(-2px);
  }

  /* Headings */
  .footer-heading {
    font-size: 0.95rem;
    font-weight: 700;
    color: #ffffff;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    margin-bottom: 1.25rem;
  }

  /* Navigation Links Lists */
  .footer-links {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .footer-links a {
    text-decoration: none;
    color: var(--text-muted);
    font-size: 0.9rem;
    font-weight: 500;
    transition: color 0.2s ease;
  }

  .footer-links a:hover {
    color: var(--brand-primary);
  }

  /* Contact Details Column */
  .footer-contact-item {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    color: var(--text-muted);
    font-size: 0.9rem;
    margin-bottom: 0.85rem;
    line-height: 1.5;
  }

  .footer-contact-item i {
    color: var(--brand-primary);
    margin-top: 3px;
    font-size: 0.95rem;
  }

  /* Bottom Copyright Bar */
  .footer-bottom {
    border-top: 1px solid var(--footer-border);
    background-color: rgba(0, 0, 0, 0.2);
  }

  .footer-bottom-container {
    max-width: 1140px;
    margin: 0 auto;
    padding: 20px 24px;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .copyright-text {
    color: var(--text-muted);
    font-size: 0.875rem;
    margin: 0;
  }

  .legal-links {
    display: flex;
    gap: 1.5rem;
  }

  .legal-links a {
    color: var(--text-muted);
    font-size: 0.875rem;
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .legal-links a:hover {
    color: var(--brand-primary);
  }

  /* Responsive Layout Adjustment */
  @media (max-width: 992px) {
    .footer-container {
      grid-template-columns: 1fr 1fr;
      gap: 2.5rem;
    }
  }

  @media (max-width: 576px) {
    .footer-container {
      grid-template-columns: 1fr;
      padding: 40px 20px 30px;
    }

    .footer-bottom-container {
      flex-direction: column-reverse;
      gap: 1rem;
      text-align: center;
    }
  }
</style>

<footer>
  <div class="footer-container">
    <!-- Column 1: Brand Summary -->
    <div class="footer-brand">
      <img class="footer-logo" src="/images/logo.png" alt="Quickplots Logo">
      <p class="footer-description">
        Simplifying real estate and plot dealings with transparent, verified listings and seamless digital processes.
      </p>
      <div class="social-group">
        <a href="#" class="social-icon" aria-label="Facebook"><i class="fa-brands fa-facebook-f"></i></a>
        <a href="#" class="social-icon" aria-label="Twitter"><i class="fa-brands fa-x-twitter"></i></a>
        <a href="https://www.instagram.com/science__world8" target="_blank" class="social-icon" aria-label="Instagram"><i class="fa-brands fa-instagram"></i></a>
        <a href="#" class="social-icon" aria-label="YouTube"><i class="fa-brands fa-youtube"></i></a>
        <a href="https://www.linkedin.com/in/junaid-alam7484" target="_blank" class="social-icon" aria-label="LinkedIn"><i class="fa-brands fa-linkedin-in"></i></a>
      </div>
    </div>

    <!-- Column 2: Quick Links -->
    <div>
      <h5 class="footer-heading">Quick Links</h5>
      <ul class="footer-links">
        <!-- Fixed: Leading slash prevents path duplication -->
        <li><a href="/quickplots/info">About Quickplots</a></li>
        <li><a href="/quickplots/post">Properties</a></li>
        <li><a href="#">Blog & News</a></li>
        <li><a href="#">FAQs</a></li>
      </ul>
    </div>

    <!-- Column 3: Services / Support -->
    <div>
      <h5 class="footer-heading">Support</h5>
      <ul class="footer-links">
        <li><a href="/quickplots/contact">Help Center</a></li>
        <li><a href="/quickplots/contact">Contact Us</a></li>
        <li><a href="#">Buy Land</a></li>
        <li><a href="#">Sell Land</a></li>
      </ul>
    </div>

    <!-- Column 4: Office Details -->
    <div>
      <h5 class="footer-heading">Contact Info</h5>
      <div class="footer-contact-item">
        <i class="fa-solid fa-location-dot"></i>
        <span>Dwarikapuri Road No.1, Danapur Patna, Bihar</span>
      </div>
      <div class="footer-contact-item">
        <i class="fa-solid fa-envelope"></i>
        <span>contact@quickplot.com</span>
      </div>
      <div class="footer-contact-item">
        <i class="fa-solid fa-phone"></i>
        <span>+91 8340252282</span>
      </div>
    </div>
  </div>

  <!-- Bottom Legal Bar -->
  <div class="footer-bottom">
    <div class="footer-bottom-container">
      <p class="copyright-text">&copy; 2026 Quickplots. All rights reserved.</p>
      <div class="legal-links">
        <a href="#">Privacy Policy</a>
        <a href="#">Terms of Service</a>
        <a href="#">Cookie Policy</a>
      </div>
    </div>
  </div>
</footer>